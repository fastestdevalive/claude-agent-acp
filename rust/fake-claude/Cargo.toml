[package]
name = "fake-claude"
version = "0.1.0"
edition = "2021"
description = "Replays recorded stream-json transcripts to stand in for the claude binary"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[dev-dependencies]
serde_json = "1"

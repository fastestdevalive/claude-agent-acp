# Review 03 — quick sanity pass (plan @ `3cc61bb`)

- Scope: blocker/major only. The implementer sees one phase block plus its Files rows and `rust/AGENTS.md`. It does not see Key Decisions, B1/B2, the Guard table or the INV registry (review-01 #4).
- Verified OK:
  - **Round-1/2 fixes are present.** Phases are numbered 0–13. Every INV phase column matches an `inv_NN_*` test in that phase's verify block (INV-6 → 3.T7 + 12.T2; INV-24 → 1, 8–11, 13). The Change Map matches the Files table. `pause_after: [2, 6]` matches `.vibekit/config.yaml`.
  - **Offline setup works.** `~/.cargo/registry` has `agent-client-protocol-2.1.0` and `-schema-1.7.0`. ACP 2.1.0 depends on schema `=1.7.0`, so pinning ACP also pins the schema. The `x86_64-pc-windows-gnu` and `aarch64-apple-darwin` targets are installed. Tag `v0.70.0` exists, and the G7 command prints nothing today.
  - **No stale references.** `bidirectional_needs`, Q3, Q6 and the old phase ids (3b, 0a, …) are gone. `argv.json` is used consistently as `<name>.argv.json`.

| # | Severity | Section | Finding | Recommended change |
|---|----------|---------|---------|--------------------|
| 1 | major | Phase 0 Context / 0.1–0.4 | **Phase 0 can't be implemented cold.** Its items point to D8, D10, G5, G8, G9 and G10, and the implementer can't see any of them. `rust/AGENTS.md` doesn't exist yet because 0.2 creates it. The Context line elides the G8 targets (`cargo check --target …`). The implementer therefore can't derive: the two target triples; the G9 regex; the G10 `cargo tree` commands; the `clippy.toml` type/method list; the `deny(unwrap_used, expect_used)` + `cfg_attr(test, allow)` lint lines; or the AGENTS.md content (the protocol steps, `rust-coding` §1–5/7/9/10 apply, §6 → the Node adapter's frames, §8 N/A). `rust-gate.sh` and `AGENTS.md` are the files every later phase depends on. | Put these into the Phase 0 Context verbatim: the G8 targets, G9 regex, G10 commands, G5 list, D8 lint lines and D10 section mapping. Also give AGENTS.md's required headings: protocol steps, skill mapping, and "record deviations in Key Decisions". |
| 2 | major | G9 (Guard table) | **G9 always passes as written.** Inside the markdown table the regex is `'cfg!?\(.*(unix\|windows\|target_)'`. Copied literally, GNU `grep -E` reads `\|` as a literal pipe, so the pattern never matches (tested: `cfg!(unix)` → no hit, rc=1). The table escaping becomes a guard that can never fail. | Take the regex out of the table as a code block: `grep -rEn 'cfg!?\(.*(unix|windows|target_)' rust/claude-agent-acp-rs/src/ \| grep -v '/process.rs:'` should be empty. Put the same line in the Phase 0 Context (#1). |
| 3 | major | G2 vs INV-25 / INV-33 | **G2 fails mechanically in phase 0 and on every phase with INV-25.** G2 wants an `fn inv_NN_` for each INV assigned to the phase. INV-33 (phase 0) is proven by the `selftest-gate.sh` script (0.T4), and INV-25 ("all") by the gate (3.T11 is labelled `INV-25`, not `inv_25_`). Neither has a test fn, so `grep "fn inv_33_"` / `"fn inv_25_"` finds nothing and the sonnet orchestrator can loop through respawns. | In the registry, mark INV-25 and INV-33 as **gate-proven (no test fn)** and add that exemption to G2. |
| 4 | major | G3 / G10 / G1 commands | **Some guard commands fail when run from the fork root.** G3 (`timeout 180 cargo test -p …`) and G10 (`cargo tree -p agent-client-protocol`, `cargo tree -d`) assume the working directory is `rust/`. G1 and G7 assume the fork root. There is no root `Cargo.toml` (it's a TS repo), so run from the root, G3/G10 fail with "could not find Cargo.toml". The G10 text also doesn't say how to check the schema pin. | Write each command with `--manifest-path rust/Cargo.toml`, or `cd rust &&`. For G10 use `cargo tree --manifest-path rust/Cargo.toml -i agent-client-protocol@2.1.0 -e normal` plus `cargo tree -d … \| grep -c agent-client-protocol` = 0. The schema is covered transitively (`=1.7.0`), so say that. |

**Minors:**
- 3.T1 compares argv **and env** with the fixture, but the fixture's env holds the capture machine's inherited vars (PWD, `npm_*`, …), so it will be flaky. Compare only the B2 env keys (`CLAUDE_CODE_ENTRYPOINT`, `CLAUDE_CODE_EMIT_SESSION_STATE_EVENTS`, `NODE_OPTIONS` absent).
- The verify items are out of order: 8.T6 comes before 8.T5, and 12.T5 before 12.T3.
- 0.6 seeds `SYNC.md` from "§ Sync workflow", which is in the plan, not in `EVALUATION.md`, so the implementer can't see it. Inline the 4 bullets or point to `porting/EVALUATION.md` if a copy exists there.
- Phase 2 needs `npm ci` (network or a warm npm cache), and 0.T2 needs the registry index for `--precise 2.2.0`. Only 0.T2 has an offline skip. Add a note for 2.2 as well.
- 1.3 puts `diff_frames` in `C/tests/differential.rs`, an integration-test file. Phases 8–13 reuse it, so say it's a `mod` shared from `tests/common/`, or keep it in the file itself, so nobody makes it `pub` in `src/`.

**Verdict: `go-with-fixes`.** Fix #1–#4 before phase 0 starts. They are all text edits to the plan and need no design change. Phases 1–13 are consistent as written.

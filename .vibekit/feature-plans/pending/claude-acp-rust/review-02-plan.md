# Review 02 — `plan-claude-acp-rust.md` (after round-1 resolutions)

- Scope: only issues that are new or still open after the 41 accepted round-1 findings. Round-1 resolutions were spot-checked and hold unless listed below.
- `P` = the plan (997 lines) · `ADP.js` = `~/.bun/install/cache/@agentclientprotocol/claude-agent-acp@0.70.0@@@1/dist/acp-agent.js` · `SDK` = `~/.bun/install/cache/@anthropic-ai/claude-agent-sdk@0.3.232@@@1/sdk.mjs` (`L:C`)
- Newly verified and correct: R27 (`ADP.js:4858` sets `CLAUDE_CODE_EMIT_SESSION_STATE_EVENTS: "1"`), R28, R29, R32, R33, and the tokio `process_group`/`creation_flags`/`kill_on_drop` line numbers.

| # | Severity | Section | Finding | Evidence (path:line) | Recommended change | Resolution |
|---|----------|---------|---------|----------------------|--------------------|------------|
| 1 | blocker | D13 transcript shape / phase 2 | The Node adapter only activates a turn when an echoed `user` frame's `uuid` equals the random `promptUuid` it wrote to stdin (`findUnsettledTurn(message.uuid)`). D13 only substitutes `$REQ` (`request_id`), and the example echo has no `uuid`. Against the fake, Node never activates a turn, so every prompt script hangs until the force-cancel floor fires. The fixtures would then record that hang as the "truth" | `P:279-286`; `ADP.js:990-991` (`userMessage.uuid = promptUuid`), `ADP.js:1517`, `ADP.js:3018-3022`, `ADP.js:3092-3101` | Generalise substitution: `emit` values may reference fields of the matched stdin frame (e.g. `$MATCH.uuid`, `$MATCH.request_id`). Show an echo carrying `"uuid": "$MATCH.uuid"` in the D13 example. Add a 2.T test: Node's text-only script reaches `stopReason: end_turn`, not a timeout | accepted — D13 gains `$MATCH.<field>` substitution, echo example carries `uuid`; 2.T5 asserts Node text-only ends `end_turn` |
| 2 | major | B2 flag table / 2.T4 / 3.T1 | B2 is missing `--session-id <uuid>`. For every non-resume session the adapter generates a `randomUUID()` and passes it as `options.sessionId`, so B1's "`sessionId` = Claude's native id" is really the adapter's own id handed to the CLI. Also, `--resume`, `--model`, `--permission-mode` and `--mcp-config` appear only under some conditions (the SDK pushes `--mcp-config` only when servers are non-empty). A single `argv.json` therefore cannot "contain every flag row" (2.T4), and 3.T1's exact-equality check will fail on the random uuid | `P:456-473`, `P:632`, `P:654`, `P:440`; `ADP.js:4707-4713`, `ADP.js:4983-4985`; `SDK:118:5883` onward (`--mcp-config` only if non-empty; `--setting-sources=<csv>` is a single `=` token) | Add a `--session-id` row, owned by phase 8. Capture `argv.json` per corpus script. 2.T4: the union of the captured argvs covers every B2 row. 3.T1: compare after normalising uuids. Write B2 flags in their exact token form (`--setting-sources=user,project,local`) | accepted — B2 gains `--session-id=<uuid>`, exact `=` token forms, conditional flags marked; argv captured per script, 2.T4 = union, 3.T1 normalizes uuids; 8.4/8.T5 added |
| 3 | major | Line-number space for `ADP` | Line 24 defines `ADP` as `src/acp-agent.ts`, but every `ADP <range>` in phases 6–10 (`1338–1682`, `2532–2884`, `6101–6193`, `4069–4275` …) uses **dist** line numbers. The `.ts` is 9,040 lines, and `src/acp-agent.ts:1338` is just `},`. `dist/` does not exist in the worktree until phase 2's `npm run build`. A phase-6 implementer following the Context will read the wrong code | `P:24`, `P:719`, `P:748`, `P:771`, `P:813`, `P:725`; `src/acp-agent.ts` (9,040 lines, no `dist/`) | Pick one space. Either re-cite every range against `src/acp-agent.ts` at `v0.70.0`, or define `ADP.js` = `dist/acp-agent.js` explicitly and add item N.0: "run `npm ci && npm run build` if `dist/` is absent" | accepted — header now defines all line refs as `dist/acp-agent.js`; items 6.0–11.0 say to build `dist/` if absent |
| 4 | major | Phase 9 tool list | The tool list does not match upstream `tools.js` at v0.70.0. `MultiEdit` and `NotebookEdit` have no case there. `Agent`, `ExitPlanMode`, `Skill`, `AskUserQuestion`, `TaskCreate`/`TaskGet`/`TaskList`/`TaskUpdate`, `ReportFindings` and the `Other` fallback are missing. The implementer would invent shapes for two tools and fall back to generic for the others, so 9.T3 diffs fail or become meaningless | `P:793-794`; `ADP.js`'s sibling `tools.js` `case` labels at lines 19–319 | Copy the actual `case` list from `src/tools.ts` at v0.70.0 into the Context. Mark any tool out of scope as `skipped-deliberate` in `PARITY.md` | accepted — phase 9 Context lists the real `case` labels (verified in `src/tools.ts`); MultiEdit/NotebookEdit take the fallback |
| 5 | major | Differ ignore-list (D13, 1.3) | The ignore-list works at frame level only. Node's `initialize` and `session/new`/`load` **responses** carry `authMethods`, `modes`, `models` and `configOptions`, all of which the Rust side skips on purpose (B1, D3). Those frames exist on both sides, so a frame-level ignore cannot hide the difference, and every corpus script will fail its first two frames | `P:598`, `P:603`, `P:609`, `P:445`; `ADP.js:727` (`authMethods`), `ADP.js:4641`, `ADP.js:4652-4653`, `ADP.js:4671-4672` | Make the ignore-list JSON-path based (e.g. `result.configOptions`, `result.modes`, `params.update[sessionUpdate=usage_update]`). Add a 1.T test: a field on an ignored path that differs still passes | accepted — ignore-list is JSON-path based (1.3, 1.T3, D13 text) |
| 6 | major | G7 / 0.T3 — fails today | The worktree's root `.gitignore` is modified (adds `.claude/`, `.vibe-station/`). That is an upstream file, and it is not excluded by G7's pathspec. Running the G7 command now prints `.gitignore \| 4 +++-`, so 0.T3 and 2.T5 fail before any code is written | `git diff --stat v0.70.0 -- . ':!rust' ':!porting' ':!skills' ':!.vibekit'` → `.gitignore`; `P:512`, `P:588` | Move those ignores to `.git/info/exclude` (or `rust/.gitignore`), or deliberately allow `.gitignore` in G7 and record the exception in D1 | accepted — verified `.gitignore` differs; G7 pathspec excludes it and D1 records the sole exception |
| 7 | major | Forward dependencies in verify blocks | Several tests need code from later phases. 5.T2 needs "25 `session/prompt` … all resolve", but ACP `session/prompt` arrives in phase 8 and turn settlement in phase 6. 6.T2 says "result text … is still **emitted**", but `SessionUpdate` emission is phase 7. 4.T6 says "after a **turn settles**", but there are no turns until phase 6. A scoped implementer will either build the later phase early or stub it, and G6/G2 will then argue about it | `P:710`, `P:706`, `P:737`, `P:690` | 5.T2: `Command::Prompt` with an injected fake turn-completion. 6.T2: assert on an internal `TurnEvent::FinalText`, not ACP emission. 4.T6: "after a `result` frame, a second `user` frame goes to the same stdin and the fake answers it" | accepted — 4.T6, 5.T2, 6.T2 reworded to use only earlier-phase code (`result` frame, `Command::Prompt`, `TurnEvent::FinalText`) |
| 8 | major | Phase 2 sizing + corpus provenance | Phase 2 asks one turn to write the replay engine, capture.sh with a Node build, and **11 hand-authored transcripts**. Hand-authoring means deepseek invents the CLI's stream-json (echo uuids, `system/init`, `session_state_changed`, `task_started`, partial `stream_event`s). Both Node and Rust are then checked against invented behaviour, and the fixtures look authoritative. The `pause_after: 2` checkpoint does not require real recordings | `P:614-633`, `P:275`, `P:994`, `P:952-953` | Split into **2a** fake-claude engine + 2.T2/2.T3 and **2b** capture + corpus. Resolve Q3 before 2b and make real recordings (`record-real.sh`) the source of transcripts. If synthetic transcripts are used for now, tag them `synthetic` in the fixture name and require re-capture before phase 13 | partially accepted — corpus provenance rule added (`*.synthetic.*` naming, re-record before phase 13, real recordings preferred); phase 2 NOT split because integer numbering and the `pause_after` config would shift again; Q3 still open and blocks only 2.4. Human may still choose to split |
| 9 | minor | Differential harness wiring | `C/tests/differential.rs` needs the `fake-claude` and `acp-recorder` binaries from **other** workspace packages. `CARGO_BIN_EXE_*` only exposes binaries of the same package, and nothing says how the test finds or builds them | `P:780`, `P:913-914` | The gate runs `cargo build --workspace --bins` before tests. The test resolves `target/<profile>/fake-claude` (via `CARGO_TARGET_DIR` or `env!("CARGO_MANIFEST_DIR")/../target`) and fails loudly if it is missing | accepted — gate runs `cargo build --workspace --bins`; test resolves `target/<profile>/…` and fails loudly (0.3, 8.6) |
| 10 | minor | G6 mechanics | Phase 0 touches `lib.rs`, which has no `#[cfg(test)]` module, so G6 fails on the first phase. G6 also ignores integration tests in `tests/`, which is where 3.T1 and 3.T5–3.T8 live | `P:511`, `P:578` | "Every `src/*.rs` file with logic touched in phase N has a unit or integration test changed in the same diff; `lib.rs` re-exports are exempt" | accepted — G6 reworded (unit or integration test; `lib.rs` exempt) |
| 11 | minor | G5 `clippy.toml` | `disallowed-types` takes concrete type paths. `tokio::sync::broadcast::*` is (likely) not accepted as a glob and would silently ban nothing | `P:510`, `P:905` | List `tokio::sync::broadcast::Sender` and `tokio::sync::broadcast::Receiver` explicitly (also consider `disallowed-methods` `tokio::sync::broadcast::channel`). Add a 0.T check that a scratch `broadcast::channel` fails clippy | accepted — `clippy.toml` lists `broadcast::Sender`/`Receiver` explicitly + `disallowed-methods` for `channel`; 0.T4 proves it |
| 12 | minor | 0.T2 / 0.T4 are mutation checks | These tests work by editing source or the lock file. They cannot live in the test suite; the orchestrator has to mutate, run and revert them. 0.T2 also needs 2.2 in the registry, so it cannot run offline | `P:587`, `P:589` | Write them as orchestrator scripts (`rust/scripts/selftest-gate.sh`) that mutate in a temp copy and always restore. For 0.T2, use `cargo update --precise` in the temp copy and skip with a notice when offline | accepted — 0.T2/0.T4 now run via `rust/scripts/selftest-gate.sh` in a temp copy; offline skip noted |
| 13 | minor | Items without a verify test | These items have no test: 4.5 `pending_permission_requests` replay, 4.6 unknown inbound subtype → error, 8.4 `session/new` option → argv mapping, 10.3 allow-always rule additions, and 12.2 shutdown on SIGTERM (12.T2 covers stdin EOF only). The INV-5 clause "user abort never hard-kills directly" is not asserted by 3.T6 | `P:681-682`, `P:778`, `P:819`, `P:861`, `P:533`, `P:659` | Add one T item for each (e.g. 4.T8 replayed `can_use_tool` reaches the handler; 8.T5 `permissionMode` → `--permission-mode`; 3.T6 also checks that abort goes through the ladder) | accepted — added 4.T8, 4.T9, 8.T5, 10.T6, 12.T5; 3.T6 also asserts abort goes through the ladder |
| 14 | minor | Registry ↔ test names | 12.T2 is named `inv_06_…`, but the registry assigns INV-6 to phase 3 only. 5.T4 is named `inv_33_no_broadcast` but INV-33 is enforced by a lint, not a test. G2's per-phase grep will flag both as mismatches | `P:534`, `P:867`, `P:712`, `P:561` | Put `3, 12` in INV-6's phase column. Rename 5.T4 to a gate check, or make INV-33's test the scratch-clippy check from #11 | accepted — INV-6 phase column `3, 12`; 5.T4 is a gate check; INV-33 proven by 0.T4 |
| 15 | minor | Files table / phase 13 Context | The "trivial in-repo echo agent" used by 1.T4 has no file row. Phase 13 has no **Context** block, which breaks the plan's own sizing rule (P:567) | `P:610`, `P:873-889` | Add a row (e.g. `rust/acp-recorder/examples/echo_agent.rs`). Give phase 13 a Context block: where the fixtures, `PARITY.md` and the § 2 (A) table live | accepted — echo-agent row + item 1.5 added; phase 13 has a Context block |
| 16 | minor | D12 / INV-28 scope | With `process_group(0)`, a host SIGKILL closes `claude`'s stdin, so `claude` exits. But its own children (MCP servers, Bash tool subprocesses) are in the group and nobody `killpg`s it, so they can outlive the host. 3.T8 uses a fake that spawns no children, so it cannot see this | `P:262-264`, `P:556`, `P:661`, `P:952` | Narrow INV-28 to "`claude` itself" and add a Risk row for grandchildren. Or have the fake spawn a `sleep` child and assert on the whole group | accepted — INV-28 narrowed to `claude` itself; Risk 17 covers grandchildren |
| 17 | minor | `.vibekit/config.yaml` comment | `pause_after` was changed to `[2, 6]`, but the comment still says "turn-settlement cost centre (3)" | `.vibekit/config.yaml:22-24` | Update the comment to "(6)" | accepted — config comment now says (6) |

**Verdict: `approve-with-changes`**. Fix #1 before phase 2 starts, since it makes the Node side hang on every prompt. Fix #2–#8 before their owning phases run (#3, #4 and #5 before phases 6, 9 and 8 respectively; #6 and #8 before phase 0/2). The minors can land alongside.

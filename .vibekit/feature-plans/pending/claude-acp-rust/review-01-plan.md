# Review 01 — `plan-claude-acp-rust.md`

- Reviewer scope: format, citations (22 spot-checked), guards/invariants, phase sizing, technical claims, consistency
- `P` = `.vibekit/feature-plans/pending/claude-acp-rust/plan-claude-acp-rust.md` · `E` = `porting/EVALUATION.md`
- `ADP` = `~/.bun/install/cache/@agentclientprotocol/claude-agent-acp@0.70.0@@@1/dist/acp-agent.js` · `SDK` = `~/.bun/install/cache/@anthropic-ai/claude-agent-sdk@0.3.232@@@1/sdk.mjs` (`L:C`)
- `ACP` = `~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/agent-client-protocol-2.1.0/` · `VST` = `~/code/fastestdevalive/vibe-station/rust/`
- Scratch checks ran in `/tmp` and were then deleted: a workspace with a `"2.1"` crate and an `=2.1.0` crate resolves to one `agent-client-protocol v2.1.0` (`cargo tree -d` shows no ACP duplicate). The ACP crate passes `cargo check` for `x86_64-pc-windows-gnu` and `aarch64-apple-darwin`.

| # | Severity | Section | Finding | Evidence (path:line) | Recommended change | Resolution |
|---|----------|---------|---------|----------------------|--------------------|------------|
| 1 | blocker | Phases 4–6 / 7 | Phases 4, 5 and 6 each run a differential test "against the Rust binary" (4.T3, 5.T3, 6.T3). The binary and the ACP agent (`agent.rs`, `bin/`) are only created in phase 7, so these tests cannot run and G1 fails in every phase from 4 to 6 | `P:503`, `P:518`, `P:533`, `P:411`, `P:540-543` | Move a minimal `agent.rs` `serve()` + `bin/` (initialize/new/prompt only) to a new phase before 4, e.g. "3b — ACP skeleton"; or change 4–6.T3 to a lower-level differential (stream-json in → `SessionUpdate` out) that needs no binary | accepted — differentials now start at phase 8 (new ACP-skeleton phase after mapping); phases 1–7 use synthetic frames or unit tests only |
| 2 | blocker | B2 / 1.1 | The argv in B2 is only the SDK's fixed prefix. The adapter's options add flags the core depends on: `--replay-user-messages` (the adapter always passes it; echo tracking in 3.4 depends on it), `--include-partial-messages` (`includePartialMessages: true`; the `stream_event` handling in 4.2/4.4 depends on it), `--permission-prompt-tool stdio` (without it the CLI sends no `can_use_tool`, so phase 5 gets no requests), `--setting-sources user,project,local`, `--permission-mode`, `--resume`, `--model`, `--mcp-config`. A driver built from B2 as written gets no echoes, no partial messages and no permission prompts | `P:306`, `P:426`; `ADP:4911` (`"replay-user-messages": ""`), `ADP:4868`, `ADP:4875`; flag list built at `SDK:118:5883` onward (includes `permission-prompt-tool`, `include-partial-messages`, `setting-sources`, `resume`) | Add the full argv and env the adapter actually produces at `v0.70.0` to B2: a table of flag → source option → which phase depends on it. Capture it once from a real run (e.g. `DEBUG_CLAUDE_AGENT_SDK=1`) and put that capture in the fixture | accepted — B2 now a full flag/env table (incl. `--replay-user-messages`, `--include-partial-messages`, `--permission-prompt-tool stdio`, `CLAUDE_CODE_EMIT_SESSION_STATE_EVENTS`); argv captured to `argv.json`, asserted by 2.T4/3.T1 |
| 3 | blocker | Phase 0 / INV-24 / Q3 | 0.T1 wants a stable fixture across 2 runs, and INV-24 wants the Rust and Node frames equal **in order**. Neither holds against a live model: the text, tool ids, uuids and streaming chunk boundaries change every run. Q3 (live vs recorded) is still open but blocks 0.5, and no normalization rules are given | `P:416`, `P:391`, `P:675` | Resolve Q3 before phase 0. Recommended: a **fake `claude`** that replays recorded stream-json and answers control requests by echoing `request_id`. Both sides can use it, because the adapter honours `CLAUDE_CODE_EXECUTABLE` (`ADP:233-236`, `4908`) and the Rust side resolves it too (1.2). Record from real `claude` once per corpus script. Specify what is normalized (ids, uuids, timestamps) and what must match exactly | accepted — D13: fake `claude` replays transcripts for both Node and Rust; normalization defined; Q3 narrowed to who records once |
| 4 | blocker | Self-containment / Phase Guard Protocol | `/sdlc turn-implement` gives each implementer only phase N's checklist, its Files & Phase Impact rows and the guardrail skills. It does not include Key Decisions, B1/B2, the Invariant Registry or Research. Phase items point to `(D6)`, `(R4)`, `(#866)`, `INV-7` and "`dispatch_lane`-equivalent", which the implementer cannot resolve. The "Implementer must" steps (read `rust/AGENTS.md`, run the gate) are also outside the scoped prompt. Fails the "could haiku implement this cold?" test | `~/.claude/skills/sdlc/PHASES.md:44`; `P:332-341`, `P:449-455`, `P:473` | Make every phase item self-contained: inline the actual contract (field names, flags, numbers, the invariant text) in the item, or add a per-phase "Context" block in the checklist. Put the implementer protocol inside `rust/AGENTS.md`, and make item N.0 of each phase "read `rust/AGENTS.md`" | accepted — every phase has a Context block inlining contracts; implementer protocol moved into `rust/AGENTS.md` (D10) with item N.0 |
| 5 | major | Phase 0 / Change Map | The harness client is placed in `tests/harness/`. An integration-test module is not an executable, so `porting/capture.sh` (0.5) cannot "run the harness client against the Node adapter" | `P:66`, `P:408-409`, `P:590` | Make the recorder a binary: `rust/acp-recorder/` crate or `src/bin/acp-record.rs` behind a feature. Tests import it as a library. Add it to the tree and to Files & Phase Impact | accepted — recorder is a binary crate `rust/acp-recorder/` (Change Map, Files table, 1.1) |
| 6 | major | D7 / INV-12 / 2.7 | D7 treats stdin close as a per-prompt SDK one-shot decision. The SDK only closes stdin after the **first `result`** when the input stream ends (`isSingleUserTurn`, or `streamInput` finished plus `waitForFirstResult`). The adapter feeds a persistent input stream, so in this port stdin must stay open for the whole session: every later prompt is written to it. Closing stdin is the teardown signal (see #9). An implementer following D7 could close stdin after a turn and break the next prompt | `P:178-181`, `P:379`, `P:455`, `P:463`; `SDK:123:95-315`, `SDK:118:24825`, `SDK:118:20884` | Reword D7/INV-12: "stdin is closed only by `dispose()`/teardown, never after a turn". Change 2.T6 to: "after a turn settles, a second prompt is written to the same stdin and answered". Drop the `bidirectional_needs` flag or scope it to a one-shot mode | accepted — D7/INV-12 reworded (stdin closed only on teardown); 4.T6 tests a second prompt on the same stdin; `bidirectional_needs` flag dropped |
| 7 | major | D4 / D6 / Phase 5 | The design never says which task awaits the client's `session/request_permission` reply. If the session actor (D4) or the stdout reader awaits it inline, stream parsing stops and `session/cancel` cannot be handled while a prompt is pending. That is a deadlock, and the same class as #866. 2.T7 only covers outbound control requests | `P:159-176`, `P:510-518`, `P:465` | Add invariant INV-27: "No actor awaits an external round-trip inline". Permission/control handlers run in a spawned task and send their result back as a `Command`. Test: a cancel sent while a permission prompt is pending resolves the prompt as cancelled within the timeout | accepted — D14 + INV-27 + 10.T3; permission tasks are spawned |
| 8 | major | Architecture / D6 / INV-8 | The diagram shows two writers to the codec: `TU -->|user message| CD` and `CT -->|control_request| CD`. That contradicts D6's single stdin writer. Also, user messages are not control requests, but D6 only describes control requests | `P:257-258`, `P:172-176`, `P:375` | Route every stdin write (user messages, control requests, control responses, cancel) through the single writer in `control.rs`. Fix the diagram, and extend 2.T2 to interleave user messages with control requests | accepted — diagram and D6 route all stdin frames through `control.rs`; INV-8 covers mixed kinds |
| 9 | major | D12 / 1.5 / INV-5 | The kill ladder does not match the SDK's `close()`. Unix: **close stdin → wait 2 s → SIGTERM → wait 5 s → SIGKILL**; the plan leaves out the stdin close and both numbers. Windows: stdin close → 2 s + 5 s → kill (7 s total, not 5 s). The SDK also signals only the child, not a process group, so killing the group is a deliberate divergence and should be recorded as one. 1.T5 cannot check "after grace" without a stated grace | `P:224-230`, `P:430`, `P:372`, `P:439`; `SDK:118:12655` (`close()` ends stdin), `SDK:118:13755` (`kAe=2000` delay, win32 5000 ms, unix SIGTERM then 5000 ms SIGKILL) | Put the exact ladder and constants in D12 and 1.5. Make the durations injectable for tests. Record "process group (SDK signals child only)" as a Key Decision | accepted — D12/R25 give the exact ladder and constants, injectable `Timings`; process-group divergence recorded |
| 10 | major | D12 / lib.rs `forbid(unsafe)` | The Change Map requires `forbid(unsafe)`, but `setsid` needs an unsafe `pre_exec`, raw `killpg` needs `libc` (unsafe), and Windows job objects need `windows-sys` FFI (unsafe). The implementer will either break the forbid or quietly drop the feature | `P:53`, `P:227`, `P:230`; `skills/rust-coding/SKILL.md:37` | Name safe APIs: `tokio::process::Command::process_group(0)` instead of `setsid` (`tokio-1.53.1/src/process/mod.rs:790`), `nix::sys::signal::killpg`, `creation_flags(CREATE_NO_WINDOW)` (`:675`), a safe job-object crate (e.g. `win32job`), and `kill_on_drop(true)` (`:664`). Pin them under `[target.'cfg(...)'.dependencies]` | accepted — D12 names safe APIs (tokio `process_group`, `creation_flags`, `kill_on_drop`, `nix`, `win32job`); crate stays `forbid(unsafe_code)` |
| 11 | major | INV-6 / reaping | There is no Node `process.on('exit')` in-process. Rust `Drop` does not run on `std::process::exit`, panic=abort or SIGKILL of `vst-daemon`, and a child in its own process group gets no SIGHUP. INV-6 only covers `dispose()`. The real protection on parent crash is the child exiting on stdin EOF, and that is untested | `P:373`, `P:431`, `P:549`; `SDK:118:806` (`CAe` exit hook) | Add INV-28: "SIGKILL the host process → no `claude` survives after N s". Test it with a helper binary that spawns via the crate and is then SIGKILLed | accepted — INV-28 + 3.T8 (helper binary SIGKILLed); Risk 14 notes the stdin-EOF assumption |
| 12 | major | INV-6 / 1.T6 | "`pgrep` finds no child" is flaky and gives false failures: `claude` processes from other sessions on the machine (including the orchestrator's own) will match | `P:440`, `P:549` | Assert on the specific child pid or pgid: `kill(pid, 0)` returns `ESRCH`, or `/proc/<pid>` is absent | accepted — INV-6 asserts on the child pid via `kill(pid,0)` |
| 13 | major | INV-21 / 5.4 / 5.T2 | "A denied permission ends the turn" does not match upstream. Deny returns `{behavior:"deny", message:"User refused permission to run tool"}` without `interrupt`, so the model continues and the turn settles on the CLI's `result`. The invariant also has no source ("—") | `P:388`, `P:513`, `P:517`; `ADP:4263-4267`, `ADP:4190-4193` | Restate: "deny → CLI receives the deny payload (exact shape); turn continues and settles on the next `result`; no hang". Handle a client `cancelled` outcome separately | accepted — R28, INV-21, 10.T2 restated (deny payload, turn continues) |
| 14 | major | INV-17 / 3.6 vs 6.2 | The force-cancel floor is only armed by `cancel` (upstream arms it inside `cancel()`), but INV-17 and 3.T5 test it in phase 3, before cancel exists (6.1). 3.6 and 6.2 describe the same timer twice. The 30 s grace is not stated | `P:384`, `P:477`, `P:485`, `P:526`; `ADP:3593-3605`, `ADP:49` (`DEFAULT_FORCE_CANCEL_GRACE_MS = 30_000`), `ADP:586` | Move 3.6/INV-17 into phase 6. State 30 s and make it injectable (upstream makes it mutable for tests). 3.T5 then becomes: "wedged stream + cancel → settles `cancelled` at the injected grace" | accepted — force-cancel floor and INV-17 moved to phase 11; 30 s injectable (R29) |
| 15 | major | Phases — missing core ranges | Several `E` § 2 (A) core ranges have no phase item: `result` → stop-reason mapping (`2532–2884`, 353 lines), idle settle via `session_state_changed` including the #825 "idle without result fails the active turn" path (`2049–2163`), prompt content mapping ACP → Claude (`6101–6193`), and most of the tool-shape mapping (`tools.js` ~630 lines, now one line in 4.3). Phase 8.2 would find these only at the end | `E:163-176`; `P:468-504`; `ADP:2141-2153` | Add explicit items and tests: stop-reason table (subtype → `StopReason`), idle-lane settle (#825), prompt content blocks (text/image/resource/resource_link), tool kind/title/locations/diff content per tool. See #22 for placement | accepted — phase 6 covers result→stop-reason table, idle lane (#825), #453; phase 8 prompt content mapping; phase 9 tool shapes |
| 16 | major | B1 `session/load` | Upstream `loadSession` replays the transcript as `session/update` notifications through SDK `getSessionMessages` (it reads `~/.claude` JSONL). The plan says only "resume via `--resume`", so either history replay is silently dropped or someone has to port an SDK function. vibe-station calls `load_session` and reads `agent_capabilities.load_session` | `P:290`; `ADP:788-792`, `ADP:3861-3863`; `VST/vst-agents/src/acp_connection.rs:275`, `:688-689` | Decide and record it: port transcript replay (and say where the JSONL lives) or mark it `skipped-deliberate` with vibe-station's impact. Add a corpus script for load/resume | accepted — D15: resume yes, history replay `skipped-deliberate` for v0.1; Q6 asks the human whether vibe-station needs it; resume/load corpus script added |
| 17 | major | D9 dependency pin | The caret `"2.1"` does unify with `=2.1.0` inside vibe-station (checked in scratch). But the fork, built alone, resolves to the newest 2.x: `E:199` says 2.2.0 is out. The fork's CI and harness would then test against 2.2 while vibe-station ships 2.1.0, and any use of a 2.2-only API breaks the vibe-station build | `P:201`, `P:621`; `VST/vst-agents/Cargo.toml:27`; `E:199` | Commit `rust/Cargo.lock` pinned with `cargo update -p agent-client-protocol --precise 2.1.0`, add a gate check that the lock holds 2.1.0, and do the same for `agent-client-protocol-schema` | accepted — committed `Cargo.lock` pinned to 2.1.0 + G10 (D9, 0.5, 0.T2) |
| 18 | major | Guards G4/G5/G6/G9 | They are not really mechanical. G4's grep matches `#[cfg(test)]` modules inside `src/`, and "type-system-guaranteed" is a judgment call. G5 is bypassed by `Arc<tokio::sync::Mutex<SessionState>>`, `RwLock` or a type alias. G6 ("items rewritten in a previous phase") has no mechanical definition. G9 misses `cfg!(windows)`, `cfg(not(unix))` and `target_family` | `P:350-355` | G4: `#![deny(clippy::unwrap_used, clippy::expect_used)]` plus `cfg_attr(test, allow)`. G5: `clippy.toml` `disallowed-types` = std/tokio `Mutex`/`RwLock` with an allowlist. G9: regex `cfg!?\(.*(unix|windows|target_)`. G6: drop it, or define it as "every `src/*.rs` touched this phase has a `#[cfg(test)]` test that changed in the same diff". G2: require test names `inv_NN_*` so the mapping in 8.3 is greppable | accepted — G4 clippy lint (D8), G5 `clippy.toml` disallowed-types, G6 redefined, G9 regex, G2 `inv_NN_` naming |
| 19 | major | INV-10 / 2.T4 | `control_cancel_request` goes **CLI → driver**: it aborts the handler for an *inbound* control request (e.g. a pending `can_use_tool`) that the CLI no longer wants. 2.T4 ("resolves the pending future as cancelled") reads as the outbound `oneshot` map, which is the wrong direction. The citation offset is also off | `P:377`, `P:461`; `SDK:120:156` (`handleControlCancelRequest` aborts `cancelControllers[request_id]`), dispatch at `SDK:118:23800` | Restate: "an inbound `control_cancel_request` aborts the in-flight handler for that `request_id` (e.g. withdraws the ACP permission prompt) and no response is written". Re-cite `SDK:120:156` | accepted — INV-10/4.T4 restated as inbound cancel aborting the handler; citation moved to `sdk.mjs:120:156` |
| 20 | major | Phase sizing — phase 0 | Nine items for one deepseek turn: workspace, AGENTS, gate script, a Rust ACP recorder, a Node build + capture, ≥8 corpus scripts, the differ, PARITY, SYNC. The turn also depends on the unresolved Q3 | `P:399-420` | Split into **0a** scaffold (0.1–0.3, 0.8, 0.9; 0.T4, 0.T5), **0b** recorder + differ (0.4, 0.7; 0.T2, 0.T3), **0c** fake-claude + capture + corpus (0.5, 0.6; 0.T1) | accepted — phase 0 split into phases 0 (scaffold), 1 (recorder+differ), 2 (fake claude+corpus) |
| 21 | major | Phase sizing — phase 3 | This is the cost centre, but it has 7 one-line items, no state diagram, and no definition of "echo", "activate", "defer" or "orphan" for an implementer who cannot see `E`. SECTIONS.md requires a `stateDiagram-v2` for more than 2 states | `P:468-487`; `~/.claude/skills/planning/SECTIONS.md:168` | Split into **3a** session actor + dispatch (3.1, 3.2, 3.7; INV-13/14/15) and **3b** turn machine (3.3–3.5, idle lane, stop reasons). Add a Turn `stateDiagram-v2` with the transitions from `ADP:1338-1682`, including the echo ↔ `--replay-user-messages` link | accepted — phase 3 split into 5 (session+dispatch) and 6 (turn machine); Turn `stateDiagram-v2` added |
| 22 | major | Phase sizing — phases 4, 7 | Phase 4 holds the whole mapping layer plus ~630 lines of tool shapes. Phase 7 holds every ACP handler, the `session/new` option mapping (cwd, model, permission mode, `ServeOptions`), load/resume, steering, the binary, an example and shutdown | `P:491-504`, `P:538-551` | Split 4 into **4a** message/stream mapping + dedupe and **4b** tool shapes (`tools.js`). Split 7 into **7a** handlers + session options, **7b** steering + load, **7c** binary/example/shutdown + INV-26. Pair this with #1 | accepted — phase 4 split into 7 (mapping) and 9 (tool shapes); phase 7 split into 8 (skeleton), 10.., 12 (steering/shutdown/example) |
| 23 | major | B1 / 7.1 — `ServeOptions` | `serve(transport, opts: ServeOptions)` is the crate's public API, but `ServeOptions` has no fields defined: binary path, env, default cwd, log sink, grace constants. The "~1-line" vibe-station swap also has to replace the `AcpAgentConfig` spawn spec (command, env `CLAUDE_CODE_EXECUTABLE`, cwd) and the process-group teardown that `AcpAgent` gave for free | `P:282-283`, `P:203`; `VST/vst-agents/src/acp_connection.rs:19-20`, `:27`, `:384-385`, `:572` | Type `ServeOptions` (fields, types, defaults) in B1. Replace "~1-line" with the actual touch points: build the transport, pass `ServeOptions`, and move teardown ownership into the crate | accepted — `ServeOptions`/`Timings` typed in B1; D9 lists real vibe-station touch points instead of '~1-line' |
| 24 | major | Skill `rust-coding` (vendored) | The vendored skill is specific to vibe-station: `vst-*` crates, `daemon/src/types.ts` wire-compat (§6), rusqlite (§8), and the "arch doc" gotchas. A deepseek implementer told to follow it will apply §6 against the wrong source of truth | `skills/rust-coding/SKILL.md:3`, `:14`, `:74-78`, `:92` | In `rust/AGENTS.md`, say which sections apply (§1–5, 7, 9, 10). Remap §6 to "the Node adapter's ACP frames at `v0.70.0` are the wire truth". Mark §8 not applicable | accepted — D10 maps `rust-coding` sections (§6 remapped to adapter frames, §8 N/A) inside `rust/AGENTS.md` |
| 25 | major | Missing invariants | The list misses: #453 result-text fallback (latched boolean), orphan-coalescing ordering (`1453`), #825 idle-without-result settle, R14 (`broadcast` `Lagged` ≠ termination), and codec read cancel-safety (`AsyncBufRead::read_line` is **not** cancel-safe; only `Lines::next_line` is). None of these has an INV or a test | `E:261-270`; `P:109-110`, `P:366-393`, `P:478` | Add INVs for each with an owning phase. Rule: the codec runs in its own task feeding an `mpsc`; `select!` touches only `recv()`. Rule: any `broadcast` receiver treats `Lagged` as continue (or `broadcast` is banned) | accepted — added INV-29..33 (#453, orphan order, #825, codec cancel-safety, broadcast ban) |
| 26 | minor | Citations — SDK offsets | Spot-checked 12 `sdk.mjs` refs; 8 match. Off: R4 `118:4903` → the argv literal is at `118:5883`; INV-5 `118:~14900` → the ladder is at `118:12655–13800`; R3 `118:20530` → `hasBidirectionalNeeds` is at `118:20884`, and the close decision is at `123:95`; INV-10 (see #19). Match: `120:~4100` (init payload), `118:23865` (`keep_alive`), `118:704` (`kk=2048`, `AAe=200`), `118:806` (exit reaper), `118:19850` (`suppressControlResponse`), `120:~8200` (pending replay), `118:3400` (abort controller), `118:~24200` (`is_error`) | `P:99-102`, `P:372`, `P:377` | Fix the four offsets | accepted — R3/R4/R25 re-cited to `sdk.mjs:123:95`/`118:20884`, `118:5883`, `118:12655`/`13755`; INV-10 to `120:156`. INV-5 old ref removed. Offsets not re-verified by me beyond the reviewer's check |
| 27 | minor | Citations — other sources | All accurate: `ADP:4416-4420`, `3595-3605`, `3608-3648`, `179-232`, `1553-1560`, `1687-1692`, `2143-2151`, `1453`, `4109-4113`; `ACP/src/jsonrpc.rs:6471-6487` (`Channel::duplex`), `:1871-1875` (`connect_with` bound `ConnectTo<Host>`; `Channel` implements `ConnectTo<R>` for all `R` at `:6552`); `VST` `normalize.rs:340-473`, `acp_transport.rs`, `Cargo.toml:27`, `session_open.rs:125-139`, `claude.rs:414-424`; `src/acp-agent.ts` = 406,611 B; tag `v0.70.0` = `d0aafb1`. Two small errors: R8 says `mpsc::Receiver` but cites `unbounded.rs` (both are cancel-safe); R15 `transcript.rs:781` now points at post-fix code | `P:104`, `P:111` | Say `UnboundedReceiver` in R8. Cite R15 by commit `14b03f1` only | accepted — R8 says `UnboundedReceiver`; R15 cites commit `14b03f1` only |
| 28 | minor | INV-11 / 2.4 | The `initialize` payload also carries `toolAliases`, `jsonSchema`, `sdkMcpServers`, `excludeDynamicSections`, `appendSubagentSystemPrompt`, `title`, and `E` §1 row 1 lists `supportedDialogKinds`. The INV lists 4 fields and doesn't say what values the adapter sends (e.g. the `systemPrompt` preset) | `P:378`, `P:452`; `SDK:120:3633-4400`; `E:98` | List the exact fields and values the adapter sends at `v0.70.0` (take them from the fake-claude capture, #3) | accepted — INV-11 now requires equality with the captured `initialize.json` fixture rather than a hand-listed field set |
| 29 | minor | INV-14 / 3.T2 | "25 interleaved open/close" is vibe-station WS vocabulary. It is unclear whether it means sessions or prompts in this crate | `P:381`, `P:482` | "25 concurrent `session/prompt` on one session → high-water active-turn count == 1; all 25 resolve in order" | accepted — 5.T2 reworded to 25 concurrent `session/prompt` |
| 30 | minor | INV-13 / INV-15 | INV-13 says "end to end" but 3.T1 only tests the dispatch unit. 3.T3 mostly re-tests tokio's documented guarantee | `P:380`, `P:382`, `P:481`, `P:483` | Scope INV-13 to "dispatch unit" or add an end-to-end ordering test through fake-claude. Aim 3.T3 at the session's own `select!` loop, not a bare channel | accepted — INV-13 scoped to the dispatch unit; INV-15 tests the session loop's `select!` |
| 31 | minor | D5 / 3.2 | "`pub fn dispatch_lane`-equivalent" names a vibe-station function the implementer has never seen, and gives no signature | `P:166-170`, `P:473` | Give the signature (e.g. `pub fn route(msg: &StreamMsg, state: &TurnView) -> Route`) plus its runner, and name the file (`dispatch.rs` is missing from the tree and the table; 3.T1 and 6.T1 name `dispatch` and `cancel` modules that don't exist) | accepted — D5 gives the signatures; `dispatch.rs` added to tree and table; test module names aligned |
| 32 | minor | Format — required sections | There is no Requirements table, no CUJs (happy path + error path), and no Data Model line. Key Decisions use `### Dn` instead of `#### Decision N`, and only D1 has a **Where:** path | `~/.claude/skills/planning/FORMAT.md:134-141`; `P:126-234` (one `Where:` at `P:133`) | Add a numbered Requirements table and 2 CUJs as `sequenceDiagram` (prompt→settle; cancel mid-permission). Add "Data Model: N/A — nothing persisted by the crate". Add a **Where:** path to every Decision | accepted — Requirements table, two CUJ sequence diagrams, Data Model N/A line, `#### Decision` blocks each with **Where:** |
| 33 | minor | Format — Research | R1, R10, R14 and R16 are cited by no Decision or phase item (FORMAT bans uncited findings). Rows are out of order: R22 comes after R26 | `P:97`, `P:106`, `P:110`, `P:112`, `P:122`; `FORMAT.md:52` | Cite them (R14 → new INV per #25; R16 → D4; R10 → D9) or delete them. Renumber in order | accepted — every remaining R-row is cited by a Decision/INV/item (R1 non-goal, R10 D9, R14 INV-33, R16 D4); renumbered in order |
| 34 | minor | Format — process narrative | "✅ accepted / ✅ revised", "Supersedes", the ✅/⚠️ Bootstrap log and struck-through resolved Qs are process narrative, which the format bans in the plan body | `P:128`, `P:188`, `P:203`, `P:632-641`, `P:673-676`; `FORMAT.md:50`, `SECTIONS.md:257` | Keep the final decision only. Move bootstrap history to a commit or `porting/` note | accepted — status badges, Supersedes, struck-through Qs and the bootstrap log removed; bootstrap reduced to current state |
| 35 | minor | Change Map ↔ Files table | `lib.rs` is in the tree but not in Files & Phase Impact (tree ⊆ table is violated). The crate `Cargo.toml` is merged into the workspace `rust/Cargo.toml` row. The fixtures/corpus locations and `dispatch.rs` are not listed | `P:53`, `P:583`, `P:578-602` | Add rows: `C/src/lib.rs`, `C/Cargo.toml`, `porting/fixtures/`, `porting/corpus/`, the recorder crate (#5), `rust/Cargo.lock` (#17), `clippy.toml` (#18) | accepted — Files table gained `lib.rs`, `C/Cargo.toml`, `Cargo.lock`, `clippy.toml`, recorder, fake-claude, corpus, fixtures, `dispatch.rs` |
| 36 | minor | Numbering | Guards are listed G1–G5, G7, G8, G9, G6. Phase 7 lists 7.5 before 7.4. The Bootstrap table breaks at a blank line, so B-11 renders as a malformed table | `P:347-355`, `P:543-544`, `P:643-644` | Reorder. Remove the blank line before B-11 | accepted — guards re-tabulated G1–G10 in order; phase numbering redone; B-11 table fixed |
| 37 | minor | Bootstrap / Q5 — stale | B-10 is not marked done, but this worktree (`care-1`, branch `feat/rust-port`, base `parity`) exists. Q5/B-11 look answered: this worktree's session list already shows sibling agent sessions. B-10 still has the placeholder `<claude-sonet id>` (typo) | `P:642-644`, `P:677` | Mark B-10 done, record the B-11 result, close Q5 | accepted — B-10 folded into 'done at bootstrap'; B-11 result recorded; Q5 narrowed (manual spawn observed inside worktree, sdlc's own spawn not yet) |
| 38 | minor | D11 vs 8.4 | 8.4 greps `dist/acp-agent.js`, but D11 says implementers read `src/*.ts`, and `dist/` is gitignored (not present until built) | `P:562`, `P:218`; `.gitignore:2` | Grep `src/acp-agent.ts` at `v0.70.0` for `issue #` / `NOTE` / `Deliberately` | accepted — 13.4 greps `src/acp-agent.ts` |
| 39 | minor | G7 scope | G7 only protects `src/` and `package.json`. Root `AGENTS.md`, `CLAUDE.md`, `.github/`, `package-lock.json` and `tsconfig.json` are upstream files that can also drift | `P:352` | `git diff --stat v0.70.0 -- . ':!rust' ':!porting' ':!skills' ':!.vibekit'` must be empty | accepted — G7 diff widened to everything outside `rust/ porting/ skills/ .vibekit/` |
| 40 | minor | B1 steering row | `promptRequired` is only returned when the request carries `_meta.steering.idleBehavior === "promptRequired"`. Other `idleBehavior` values are rejected with `invalidParams` | `P:293`; `ADP:83-88`, `ADP:1147` | Spell out both idle behaviours and the error | accepted — B1 steering row spells out both idle behaviours + `invalidParams` (R32, verified at `acp-agent.js:80-88`, `1147`) |
| 41 | minor | D6 / 2.5 `suppressControlResponse` | This is an SDK-internal JS `Symbol` returned by SDK handlers. The plan names no Rust handler that would ever return it | `P:176`, `P:453`; `SDK:118:19850` | Say which handler declines to answer and why, or mark it `skipped-deliberate` | accepted — D6 marks `suppressControlResponse` `skipped-deliberate` (no Rust handler declines to answer) |

**Verdict: `rework`**. Blockers #1–#4 are structural: phase order vs. the binary, the incomplete argv, a differential design that can't be made deterministic against a live model, and phase items that make no sense inside `turn-implement`'s scoped prompt. Fix those and the majors, then re-review.
